1. Character Rotation Mechanism (Visual Optimization)Implement a visual turning mechanic for characters. In addition to world coordinates $(x, y)$, the system must now track the character's current facing direction. If a movement command dictates a direction different from the current facing, the character must execute a smooth rotation before moving forward, eliminating stiff translation.180° Turns: If the command requires moving backward, randomly select either a left or right 180° rotation.Constant Duration: The total animation time for the rotation plus the forward movement must equal the original single-tile translation time (dynamically increase animation playback speed as necessary).Logic Separation: This is strictly a frontend visual update; backend grid movement logic remains identical.

2. Double-Layer Terrain Generation (Anti-Hollow)Deprecate the previous "one model per tile" logic. Enforce a double-layer placement system to prevent any visual gaps or hollow spaces on the map.Base Layer: Apply a foundational ground model to all grid tiles.Surface Layer: Randomly spawn grass, rocks, or other natural structures on top of the base ground.Implementation Note: Randomly sample the .json configuration files in the assets/models/nature directory to design the surface generation and combination algorithm based on available assets. Do not read the entire directory to save context.
